# ImageTest
A C++ project made to test animations and sprites.
Eventually this might grow into a game engine.

*NOTE: Currently the required DLLs are in different folders, so you won't get the actual thing.*
